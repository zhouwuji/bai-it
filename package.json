{
  "name": "bait",
  "private": true,
  "license": "BSL-1.1",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "release": "npm test && npm run build && VERSION=$(node -p \"require('./manifest.json').version\") && cd dist && zip -r \"../bai-it-v${VERSION}.zip\" . && cd .. && node scripts/package-firefox.mjs && echo \"Packaged: bai-it-v${VERSION}.zip + Firefox zip\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.25",
    "fake-indexeddb": "^6.2.5",
    "puppeteer": "^24.37.5",
    "typescript": "^5.7",
    "vitest": "^3"
  },
  "dependencies": {
    "pos": "^0.4.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
